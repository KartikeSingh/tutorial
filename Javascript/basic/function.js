/**
 * like do colsldefnwm;okrmlenwmko[,dfw
 * * sum for 2 numbers
 * 
 * @param {*} a 
 * @param {*} b 
 * @param {*} callback 
 * @returns 
 * - 3ew
 */
function add(a, b, callback) {
    const c = a + b;
    if (typeof (callback) === "function") callback(c);

    return c;
}

// TODO add a subtract fucntion

// ? aksk roigyfebdw